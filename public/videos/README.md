Drop the optional cinematic file here as `kaidi-kitchen.mp4`.

The hero video is muted, plays inline, and is scrubbed by GSAP ScrollTrigger. If the file is not present, the generated scene images in `/public/images` are used automatically as a continuous fallback sequence.
